name: Ping on Render every 2 minutes
on:
    schedule:
        - cron: "*/2 * * * *"

jobs:
    ping:
        runs-on: ubuntu-latest

        steps:
            - name: Ping Render Service
              run: |
                  curl -X GET "https://pascom-backend.onrender.com/health"
